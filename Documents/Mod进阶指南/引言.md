# Mod进阶指南

本指南面向已经能够编写基础职业 Mod 的开发者，重点解释两个问题：

- 如何理解技能 DSL 编译后的执行链
- 如何通过动态判定规则修改回合流程

建议阅读顺序：

- [判定实现](./判定实现.md)
- [DSL与规则链接](./DSL与规则链接.md)

如果你还没有写过最基本的职业 Mod，请先阅读：

- [Mod基础指南](../Mod基础指南/引言.md)

> **注意**：编写职业 Mod 时，技能方法必须返回 `IDSLSourceFile` 接口（而非具体类 `DSL.SourceFile`），否则反射配对会失败。详见 [职业Mod](../Mod基础指南/职业Mod.md)。

如果你想先建立整体认识，再回来看这里的高级机制，可以先阅读：

- [项目架构](../项目架构.md)

## 这一部分解决什么问题

基础指南足够让你写出：

- 普通攻击
- 防御
- 资源变化
- 简单的被动技能

但当你想实现下面这些能力时，就需要理解进阶机制：

- 临时改写某个判定阶段
- 让一个技能在”下一回合”才开始生效
- 在特定阶段插入一个额外规则
- 实现转移、反击、延迟保护、阶段性强制处理等复杂效果
- 实现跨职业技能复制（Association）——在运行时”窃取”另一个职业的 DSL
- 精确控制自由句子的所有权转移行为（`WriteFree` 的 `canMove` 参数）

这些能力主要依赖：

- `DSLforSkillLogic.SourceFile`（实现 `IDSLSourceFile` 接口）
- `WriteFree` 的 `canMove` 参数与 `Move()` 所有权转移
- `ISudoOperations` 越权操作接口
- `Mutation`
- `DynamicJudgeRulePool`
- `JudgeRuleManager`（继承 `ClapJudgeRuleManager<Community>`）
- `Judger`（继承 `ClapJudger<...>`）
- `DynamicJudgeRuleName`

## 你会看到的核心结论

1. DSL 不是直接结算，而是先构造 `SourceFile`（实现 `IDSLSourceFile`）。
2. `SourceFile.Compile(judger)` 会把技能变成 `Intent`（继承 `ClapIntent<Community>`）。
3. 如果技能里使用了 `LinkJudgeRuleDynamic(...)`，那么编译阶段还会把动态规则注册到裁判系统。
4. `SourceFile.Move(newOwner)` 可以转移 DSL 的所有权，根据每个句子的 `CanMove` 标志决定保留或剥离——这是 PhantomBook Association 模式的基础。
5. 真正执行一回合时，`Judger`（继承自 ClapInfra 的 `ClapJudger`）会按多个 `JudgeStage` 依次运行基础规则、覆盖规则和前后修饰器。
